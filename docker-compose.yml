# Use postgres/example user/password credentials
version: '3.1'

services:
  db:
    image: postgres
    restart: unless-stopped
    env_file: ./.env

  adminer:
    image: adminer
    restart: unless-stopped
    ports:
      - 8081:8080
